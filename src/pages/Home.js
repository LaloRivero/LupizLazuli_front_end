import React from 'react';
import PhotoCarousel from '../components/PhotoCarousel';
const Home = () => {
  return (
    <React.Fragment>
      <PhotoCarousel/>
    </React.Fragment>
  );
}

export default Home;
